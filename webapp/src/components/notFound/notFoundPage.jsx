'use strict';

module.exports = function (React) {
    return React.createClass({
        render: function () {
            return (
                <div>
                    <h1>Not Found</h1>
                </div>
            );
        }
    });
};
