'use strict';

module.exports = function (React, songsStore) {
    return React.createClass({
        render: function () {
            return (
                <div>
                    <h1>Playlist</h1>
                </div>
            );
        }
    });
};
